
#ifndef MY_BOOL_H
#define MY_BOOL_H

/**
 * @brief Boolean type representing true or false values.
 *
 * @note The `bool_t` type is an enumeration that represents the two possible
 * boolean values, `true` and `false`. This type is commonly used in C and C++
 * programs to represent logical values.
 */
typedef enum bool_t { false = 0, true = 1 } bool_t;

#endif